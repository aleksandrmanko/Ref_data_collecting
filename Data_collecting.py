import os
import openpyxl

#Program coolecting data from excel tables in one

#Collecting data from tables in dictionary

print('Enter the number of tables ')
n = input() #Number of tables
N = n + 1 #For circle

for T in range(1,N):
